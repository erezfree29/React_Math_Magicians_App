 const oldState = this.state;
    this.setState({
      obj: {
        next: calculate(oldState, buttonName).next,
        operation: calculate(oldState, buttonName).operation,
        total: calculate(oldState, buttonName).total,
      },
    });













    this.setState({
      obj: {
        total: 6,
        next: 7,
        operation: '+',
      },
    });